{
  "name": "arqen",
  "private": true,
  "version": "0.2.0",
  "packageManager": "pnpm@10.15.0",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "typecheck": "tsc -b --pretty false"
  },
  "devDependencies": { "typescript": "^5.9.2", "vitest": "^5.0.0" }
}
